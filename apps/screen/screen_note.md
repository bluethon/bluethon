Linux Screen Note
=================

process background

``` shell
screen                      # first
netease-cloud-music %U      # run
screen -ls                  # list
screen -r foo               # attach
```

Ctrl+A and Ctrl+D, and session后台运行
